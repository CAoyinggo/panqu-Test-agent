# DevTest 通用测试实施说明模板

> 本文用于说明一次需求驱动测试的输入、能力绑定和交付边界，不定义产品、项目或单功能流程。

## 1. Scope

| 字段 | 内容 |
| --- | --- |
| Requirement Reference | `{requirementRef}` |
| Version / Source | `{sourceRef}` |
| Business Scenario References | `{scenarioRefs}` |
| Included Facts / Risks | `{factAndRiskRefs}` |
| Explicit Exclusions | `{excludedScope}` |

## 2. Standard Chain

```text
Requirement Model
→ Business Model
→ Business Scenario
→ Dynamic Test Dimensions
→ TEST_CASE_V2
→ Runtime Readiness
→ Execution / Evidence / Oracle
→ Report
```

## 3. Runtime Binding

| Capability | Runtime Reference | Required | Availability | Missing Impact |
| --- | --- | --- | --- | --- |
| Environment | `{environmentRef}` | `{true/false}` | `{available/unavailable}` | `{impact}` |
| Processor | `{processorRef}` | `{true/false}` | `{available/unavailable}` | `{impact}` |
| Observer | `{observerRef}` | `{true/false}` | `{available/unavailable}` | `{impact}` |
| Test Data | `{dataRef}` | `{true/false}` | `{available/unavailable}` | `{impact}` |
| Prepare/Cleanup | `{hookRef}` | `{true/false}` | `{available/unavailable}` | `{impact}` |
| Dependency | `{dependencyRef}` | `{true/false}` | `{available/unavailable}` | `{impact}` |

运行时绑定值不得写回通用模板。凭据和 Secret 只能由安全配置解析。

## 4. Dynamic Test Dimensions

列出本次 Requirement 实际触发的测试能力及来源，不复制固定功能清单：

| Capability | Trigger Facts / Risks | Cases | Evidence Channel |
| --- | --- | --- | --- |
| `{capability}` | `{refs}` | `{caseRefs}` | `{channels}` |

## 5. Execution Ledger

| Case | Generated | Executable | Executed | Verified | Status | Evidence Gap |
| --- | --- | --- | --- | --- | --- | --- |
| `{caseRef}` | `{yes/no}` | `{yes/no}` | `{yes/no}` | `{yes/no}` | `{status}` | `{gap}` |

不得手填 PASS。所有结果来自 Runner、Assertion 和 Evidence。

## 6. Business Coverage

| Coverage | GENERATED | EXECUTABLE | EXECUTED | VERIFIED |
| --- | --- | --- | --- | --- |
| Business Flow | `{value}` | `{value}` | `{value}` | `{value}` |
| State | `{value}` | `{value}` | `{value}` | `{value}` |
| Permission | `{value}` | `{value}` | `{value}` | `{value}` |
| Isolation | `{value}` | `{value}` | `{value}` | `{value}` |
| Side Effect | `{value}` | `{value}` | `{value}` | `{value}` |

## 7. Issues / Unknowns / Legacy Boundary

| Type | Reference | Description | Impact | Resolution |
| --- | --- | --- | --- | --- |
| `{issue/unknown/out-of-scope}` | `{ref}` | `{description}` | `{impact}` | `{resolution}` |

## 8. Reproduction and Regression

- Requirement and execution-plan digest: `{digestRefs}`
- Selected Case references: `{caseRefs}`
- Evidence archive reference: `{archiveRef}`
- Regression selection reason: `{reason}`
- Cleanup result: `{result}`

## 9. Standardization Declaration

- 使用统一 Requirement Model、Business Model 和 TEST_CASE_V2。
- 未增加项目专属字段、Test Type、业务流程或协议。
- 标准化 Gate、Quality Gate 和 Runtime Readiness 结果已记录。
