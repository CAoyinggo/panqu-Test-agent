# LEGACY 模板合集（测试盘点 / 数据需求 / 启动检查）

> 版本：v1.1 ｜ 更新：2026-08-27 ｜ 维护：AI 测试智能体
> 本文件是历史汇总资产，保留用于老任务迁移、数据盘点与启动沟通。它不是当前 DevTest TestCase 执行协议。
> **通用模板说明**：以下均为通用模板，使用时请将所有 `{占位符}` 替换为实际业务数据。示例以 `wan3`（视频生成）业务为例，仅作演示，实际使用时请替换为您的业务名（如 user、order、payment）。
> **编号规则**：`{MODULE}-{CATEGORY}-{序号}`——`{MODULE}` 为用户自定义业务缩写（如 wan3、user、order），`{CATEGORY}` 为用例分类（FUN 功能 / INT 接口 / BILL 计费 / ISO 数据隔离 / UI 浏览器）。

---

## 一、LEGACY 测试用例盘点表

> **非主链模板**：下表只能用于迁移前盘点，不能直接进入 Runner，不记录手填 `PASS/FAIL/待人工`。新增或更新用例必须使用 [DevTest TestCase V2 字段与生成规则](testing/testcase-v2-schema.md)，或编辑 [`tests/acceptance/templates/scenario.md`](../tests/acceptance/templates/scenario.md) 后通过 canonical Gate。

Legacy 行必须显式转换为 Requirement/Fact trace、可执行 Step、确定性 Assertion/Oracle、Evidence Requirement、Lifecycle 和 Readiness。“API / 浏览器 / SQL”只表示预期通道，不证明 Processor/Observer 已接入。

### 用例头信息

```yaml
任务名: {任务名}
业务类型: {业务场景}   # 示例：{业务场景}
测试环境: {test / preonline}
项目ID: {项目ID}       # 不同环境值不同，按实际环境填写
提交人: {账号}
关注点: {本次重点验证的内容}
版本/日期: {提测版本} / {日期}
```

### 盘点清单（仅供迁移）

| Legacy ID | 优先级 | 分类 | 需求/AC 来源 | 测试意图 | 已知前置 | 候选操作 | 候选预期 | 预期通道 | 迁移状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `{MODULE}-FUN-01` | P0 | 功能 | `{REQ/AC 引用}` | `{提交业务任务}` | `{身份/数据/环境}` | `{候选步骤}` | `{来自需求的预期}` | API/UI/DATA | `UNMIGRATED / MIGRATED / BLOCKED` |

`MIGRATED` 只表示已转换为 V2/canonical Scenario，不表示已执行或已通过。真实结果只能由 Runner + Evidence + Oracle 产生。

---

## 二、数据需求清单模板

> 用途：每次测试执行前，明确本次测试需要哪些数据、来源是什么（可复用 / 需你提供），避免执行中断。

### 清单格式

| # | 数据项 | 用途 | 来源（可复用 / 需提供） | 状态（已备 / 待提供） | 备注 |
|---|---|---|---|---|---|
| 1 | 登录态（auth cookie + JWT） | API 鉴权 | 可复用：`test-Configuration/session-cookies.json` | 已备 | 含 test / preonline 两环境 |
| 2 | 项目 ID | 提交/查询任务 | 可复用（随登录态） | 已备 | 各环境值不同（test={项目ID}、preonline={项目ID}），按实际环境填写 |
| 3 | 任务名 | 任务标识 | 需提供（需求输入） | 待提供 | |
| 4 | 业务场景类型 | 选择任务类型/接口 | 需提供（需求输入） | 待提供 | 按实际业务填写，示例：{业务场景} |
| 5 | 输入内容（如提示词） | 生成内容 | 需提供（需求输入） | 待提供 | 按实际业务决定是否必填 |
| 6 | 参考素材地址 | 有参考素材的业务 | 可复用：测试素材库 `/Users/mac/agents/Test-panqu/` | 待确认 | 图片 `photo/`、音频 `audio/`、视频 `video/`；脚本自动扫描上传 |
| 7 | 参数（如分辨率/时长/比例） | 提交参数 | 需提供（需求输入） | 待提供 | 示例：{分辨率} / {时长} / {比例} |
| 8 | 计费规则 | 计费断言 | 可复用（代码/已接入清单） | 已备 | 示例：{单价}，如 720P=60积分/秒 |
| 9 | CSRF token | 提交鉴权 | 脚本自动获取 | 已备 | 从页面 HTML 提取 |
| 10 | 数据库访问 | SQL 查库（可选） | 需提供（如需深度验证） | 待提供 | 可选 |

### 使用流程

1. **我填写清单**：根据需求与代码分析，列出本次需要的全部数据项，并标记来源。
2. **你确认/补充**：逐项确认；标"需提供"的项目由你补充（尤其素材路径、参数）。
3. **迁移**：将清单显式转换为 TestCase V2 `testData[]` / `preconditionPlan[]` / `dependencies[]`，不再新建 `tasks/*.json`。
4. **执行**：清单与 Preflight 全部就绪后，使用 `npm run devtest -- requirements/{需求名}.md`；缺失必需数据时 fail-close。

### 示例（{业务模块}{业务场景}-{示例任务}，wan3 仅作演示）

| # | 数据项 | 来源 | 状态 | 备注 |
|---|---|---|---|---|
| 1 | 登录态 | 可复用 session-cookies.json | 已备 | test 环境 DEL01 |
| 2 | 项目 ID | 随登录态 | 已备 | {项目ID}（示例：365） |
| 3 | 任务名 | 你提供 | 已确认 | {业务模块}{业务场景}-{示例任务} |
| 4 | 业务场景类型 | 你提供 | 已确认 | {业务场景}（{任务类型编码}，示例：105） |
| 5 | 输入内容（提示词） | 你提供 | 已确认 | {提示词内容}（示例：夕阳下的城市天际线，无人机航拍） |
| 6 | 参考素材 | 不适用 | - | 纯文本生成业务不需要素材 |
| 7 | 参数 | 你提供 | 已确认 | {分辨率} / {时长} / {比例}（示例：720p / 4秒 / 9:16） |
| 8 | 计费规则 | 可复用 | 已备 | {示例扣费}（示例：4秒×60=240 积分） |
| 9 | CSRF | 脚本自动 | 已备 | |

---

## 三、新任务启动检查清单模板

> 用途：每个新任务开始前的强制门槛。我按本模板输出启动清单，你逐项确认后，才进入用例编写与执行阶段。
> 位置：本清单随 Requirement 输入一并管理，生成 TestCase V2；Legacy `tasks/*.json` 只供迁移。

---

### 启动清单

**任务名**：{任务名}
**日期**：{日期}

#### 1. 业务场景类型确认

> 以下为示例场景，**按本次业务实际填写**（user/order/payment 等业务可增删改场景项）。

- [ ] {业务场景}（例如：提示词 → 产物）
- [ ] {业务场景}（例如：参考素材 → 产物）
- [ ] {业务场景}（例如：文本 → 分镜/镜头）
- [ ] {业务场景}（例如：账务/计费核验）
- [ ] {业务场景}（{业务场景说明}）
- [ ] 其他（请注明）：{补充}

**场景确认结果**：{场景类型}（影响脚本执行逻辑与任务定义字段）

#### 2. 所需数据核对

| 数据项 | 来源 | 状态（已备/待提供） | 备注 |
|---|---|---|---|
| 登录态 | 可复用：`../test-Configuration/session-cookies.json` | 已备 | test / preonline 两环境 |
| 项目 ID | 随登录态 | 已备 | 各环境值不同（test={项目ID}、preonline={项目ID}），按实际环境填写 |
| 任务名 | 需提供（本次需求） | 待确认 | |
| 业务场景类型 | 需提供（本次需求） | 待确认 | 按实际业务填写 |
| 输入内容（提示词等） | 需提供（本次需求） | 待确认 | 按实际业务决定是否必填 |
| 参考素材 | **可复用：测试素材库 `../Test-panqu/`** | 待确认 | 图片 `photo/`、音频 `audio/`、视频 `video/`；脚本自动扫描 |
| 长文本 | **可复用：`../Test-panqu/txt/`** | 待确认 | 剧本/故事类场景读取对应 txt |
| 测试参数 | 需提供（本次需求） | 待确认 | 分辨率/时长/比例/模型（示例参数）；其他业务按实际参数填写 |

#### 3. 测试参数确认

> 说明：以下参数为示例，**按本次模块实际填写**（不同模块参数不同，可增删改）。

- [ ] 模型：{模型名 / model_id}
- [ ] 分辨率：{分辨率档位（示例：480P / 720P / 1080P）}
- [ ] 时长：{时长（示例）}
- [ ] 比例：{比例（示例：9:16 / 16:9 / 4:3 / 1:1 / 3:4 / 21:9 / 自适应）}
- [ ] 计费规则：{单价}（脚本据此断言扣费）

#### 4. 关注点确认

{本次重点验证的内容，如：提交链路 / 计费准确性 / 数据隔离 / 状态流转 / 错误处理}

#### 5. 脚本覆盖确认

- [ ] 可执行 Processor：{API / UI / DATA / QUEUE / PROVIDER}
- [ ] Evidence Observer：{response / screenshot / database / state diff / log / side effect}
- [ ] 无法自动执行的项已显式标记 `BLOCKED / NEED_CONFIRMATION`

---

### 确认区（你填写）

- [ ] 以上场景类型、数据、参数、关注点已确认无误
- [ ] 补充/修正：{你的调整}

**确认后**：Requirement → TestCase V2 → Gate/Runner → Evidence/Oracle → Report。
