# Panqu AI DevTest (v6.0.0) — Coding Agent Rules

本项目必须遵守 docs/ARCHITECTURE_FREEZE.md。
未经人工明确授权，禁止改变核心架构。
交付应让接手者看懂实际操作、修改原因、行为变化和验证边界。避免重复与空话，但不要为省 Token 删除关键事实。输出内容与交付总结时，涉及改动必须严格显示具体增加多少行代码、减少多少行代码。

## 数据变更测试强制规则：必须自动连接数据库取证（人工明确授权 · 强制执行）
凡是执行涉及**真实数据变更**（包括但不限于真实任务派发、Task 落库、积分扣费/退款流水变动、状态流转等有写副作用场景）的测试，**必须强制执行以下规则**：
1. **自动连库取证**：测试执行过程中必须自动加载本目录下的 `db-credentials.json`，通过 DBeaver 同款 SSH 隧道模式（跳板机 115.191.19.88:22）安全建立 MySQL 数据库连接；
2. **物理证据核验**：必须针对目标业务表（如 `pq_aivideo_new`、`pq_volcengine_ai_task`、`pq_score_log`、`pq_user_score_log` 等）进行物理落库状态与流水数据只读比对；
3. **裁决门禁红线**：严禁仅凭 HTTP 200 或网关接口返回判断通过；若数据库未连接成功、未找到对应物理落库记录或流水不一致，必须严格 Fail-Closed 判定为 `UNVERIFIED` / `FAIL`，坚决执行零假 PASS；
4. **验证只读原则**：数据库取证操作必须严格保持 `READ_ONLY`（只允许 SELECT 查询），严禁向数据库执行任何修改操作（INSERT/UPDATE/DELETE）。

## 任务交付总结（强制）
每次任务结束，回复末尾附独立的 ```markdown 代码块。各项可写多条，以说清事实为准；无内容写“无”。输出内容时必须严格显示增加多少行代码，减少多少行代码。

```markdown
# [CODEX_HANDOFF] 任务名称
STATUS: COMPLETE | PARTIAL | BLOCKED
GOAL: 本次目标与实际完成范围
DONE:
- 实际操作：对什么做了什么；重要决策说明原因
CHANGED:
- 文件路径 (+增加X行 / -减少Y行)：具体改动；解决的原问题及现在的行为/效果（必须严格注明增加与减少的代码行数）
VERIFIED:
- 本次执行的命令或远端运行链接：结果、关键数字及验证范围
RISKS:
- 未解决问题、未验证范围或必须保持的架构约束
NEXT:
- 下一步可执行动作；明确本次未做且不应擅自做的事
```

`DONE` 写执行过程，`CHANGED` 写改动及价值，`VERIFIED` 写证据，不要用一项替代另一项。`CHANGED` 项输出内容时必须严格显示增加多少行代码、减少多少行代码（格式如 `(+增加X行 / -减少Y行)` 或 `(+X / -Y 行)`），每个变更文件均须独立精确量化统计，严禁遗漏或使用“微调”“修改”等模糊描述。只把本次亲自运行或查到的结果写入 `VERIFIED`；历史结果注明“沿用”，未运行写“未运行”。远端 CI 附链接与结论。目标未完成用 `PARTIAL`/`BLOCKED`，在 `RISKS` 写明缺口。不要写“已优化”“全绿”等没有对象、命令或结果的空话。
